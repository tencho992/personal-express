<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Q&A forum</title>
  <link rel="shortcut icon" href="demoday.jpeg">
  <link rel="stylesheet" href="font-awesome.min.css">
  <link rel="stylesheet" href="styles.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Roboto+Mono&display=swap" rel="stylesheet"></head>
<body>
  <section id="left">

  <section id="head">
  <p>Utilize this forum to ask further details on location, requirements, dealines, and more. Community members, Alumni and Mentors will answer any questions they can.</p>
  <strong>May the Code be with you</strong>

</section>
<section id="mid">

  <h2>Ask a Question:</h2>

  <form action="/messages" method="POST">
    <input type="text" placeholder="name" name="name">
    <textarea id="question" rows="6" cols="30" placeholder="Type Question Here..." name="question"></textarea>
    
    <button id="submitQuestion" type="submit">Submit</button>
  </form>
</section>
</section>

<section id="user">

  <h3>Forum:</h3>
  <p>Mentors and community members, feel free to answer as well.</p>
  <ul class="messages">
    <% for (var i = 0; i < hatenotes.length; i++) { %>
      <li class="message" data-id="<%= hatenotes[i]._id %>">
        <span><%= hatenotes[i].name %></span>
        <span><%= hatenotes[i].msg %></span>
        <% if (!hatenotes[i].ansr) { %>
          <textarea id="answer" rows="6" cols="30" placeholder="Type Answer Here..." name="answer"></textarea>
          <p><button class="submitAnswer" type="button">Submit</button></p>
        <% } else { %>
          <span><%= hatenotes[i].ansr %></span>
        <% } %>
        <span><i class="fa fa-trash" aria-hidden="true" data-id="<%= hatenotes[i]._id %>"></i></span>
      </li>
    <% } %>
  </ul>
</section>



  <script src="main.js"></script>

</body>
</html>
